function FizzBizz(){
for (let index = 0; index < 100; index++) {
   
     if (index % 5 == 0 && index % 3 == 0) { 
        console.log( "fizzbizz   " + index);
     }else
     if (index % 3 == 0) {
        console.log("bizz   " + index);
     }else 
     if (index % 5 == 0) {
        console.log("fizz   " + index);
     } else (console.log(index))
    }
   }    
